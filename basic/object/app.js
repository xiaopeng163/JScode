var app = {
  version: 1.0,
  name: "test",
  getVersion: function () {
    return this.version;
  }
}

// this is a array
// var name = ["a", "b", "c"]
// alert(name.length);
// for (i=0; i<name.length; i=i+1) {
//   alert(name[i]);
// }
